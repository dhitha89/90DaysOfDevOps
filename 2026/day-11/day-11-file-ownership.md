# Main Commands Used

* sudo chown owner:group file --> change user and owner of file with one command
* sudo chgrp groupname filename --> change group of a file
* sudo groupadd groupname --> create new group
* sudo useradd ownername --> create new owner
* ls -lR --> list contents of directory recursively